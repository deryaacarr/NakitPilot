from django.apps import AppConfig


class SegmentsConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.segments"
    label = "segments"
    verbose_name = "Customer segments"
